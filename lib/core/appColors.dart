import 'package:flutter/material.dart';

class AppColors {
  //static const lightAppBar = Color(0xFFEAF6FF);
  static const lightAppBar = Color.fromARGB(255, 255, 255, 255);

  static const darkAppBarTop = Color(0xFF0A1C2C);
  static const darkAppBarBottom = Color(0xFF102F47);
  static const lightBottomIcons = Colors.black;
  static const darkBottomIcons = Colors.white;

  //static const lightBackground = Color(0xFFEAF6FF);
  static const lightBackground = Color.fromARGB(255, 255, 255, 255);

  static const darkGradientStart = Color(0xFF0A1C2C);
  static const darkGradientEnd = Color(0xFF102F47);

  static const lightTitle = Colors.black;
  static const lightSubtitle = Colors.grey;
  static const lightCurrency = Color(0xFF2F80ED);
  static const darkSubtitle = Colors.grey;

  static const white = Colors.white;
  static const black = Colors.black;
}
